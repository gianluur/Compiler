const float taxRate = 0.07;
let int totalAmount = 100;
totalAmount += (totalAmount * taxRate);
totalAmount /= 1.5;

if (totalAmount > 100) {
    print("High amount");
} else {
    print("Low amount");
}